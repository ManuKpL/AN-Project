<div class="container">
  <div class="row padded">
    <div class="home-list">
      <% ('A'..'Z').to_a.each do |letter| %>
        <%= link_to letter, deputies_path(search: letter), class: "home-list-item btn btn-primary#{check_status(letter)}" %>
      <% end %>
    </div>
  </div>
  <h1><%= @deputies.first.lastname %></h1>
  <h1><%= @deputies.last.lastname %></h1>
</div>
